[build-system]
requires	= ["setuptools", "wheel"]
build-backend	= "setuptools.build_meta"

[project]
name		= "swavepy"
version		= "0.0.2"
description	= "seismic wave modelling"
readme		= "README.md"
license		= { file = "LICENSE" }
authors 	= [{ name = "Nienke Blom" }, { name = "Will Pizii" }]
dependencies	= [ "ipympl", "matplotlib", "numpy", "numba" ]

